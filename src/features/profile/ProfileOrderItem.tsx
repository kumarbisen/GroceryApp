import React, { FC } from 'react'
import {Text,View} from 'react-native'


const ProfileOrderItem:FC<{item:any, index:any}> =({item,index})=>{
    return(
        <View>
            <Text> ProfileOrderItem</Text>
        </View>
    )
}
export default ProfileOrderItem;